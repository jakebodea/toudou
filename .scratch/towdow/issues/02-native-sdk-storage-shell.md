# Native SDK storage, tray, and local I/O

Type: research
Status: open

## Question

Against Native SDK primary docs/examples: how should Towdow persist Captures/Tags (SQLite vs files via effects), where do app-data + `attachments/` live, and what is the supported pattern for tray + hide-on-close, clipboard read/write (text + image), timers for 24h Done purge, and any in-app toast/overlay — so later tickets don’t guess the platform APIs?
