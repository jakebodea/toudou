# Global double-Shift + selection capture on macOS

Type: research
Status: open

## Question

What is the viable path, on macOS with Native SDK (and if needed a thin OS companion), to (1) register a **global** double-Shift (or document why it’s impossible and what the closest global hotkey is), and (2) on fire, read the **current selection** via Accessibility when available, else **clipboard**, including text and image bytes — so Toudou can create a Capture while the window is hidden?
